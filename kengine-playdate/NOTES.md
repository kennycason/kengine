# Notes

WIP 

Build & RUn

```shell
./gradlew build
cd kengine-playdate
make clean && make
make post_pdx
make run 
```

Current errors:
```
Kengine Playdate game started!
wrong file type: no header
```